# moonactive-fullstack-test-fe

To start the project, run the command `yarn` to install the required dependencies. Once the installation is complete, execute the command `yarn start` to launch the project